package graphvis.group30;

public interface MersenneTwisterRandom{
    int nextInt();
    int nextInt(int n);
    int nextInt(int min, int max); // we don't need for our purposes other types except Int
}

